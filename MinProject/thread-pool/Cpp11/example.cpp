#include "thread_pool.h"

using namespace tp;

int main()
{
    // ThreadPool thread_pool;

    // thread_pool.start();

    // task

    // auto ret = thread_pool.exec();

    // thread_pool.stop();
    
    return 0;
}